{
  "name": "track-and-trace-server",
  "version": "1.0.0",
  "description": "Middleware for Track & Trace demo app built on BlockApps STRATO",
  "main": "index.js",
  "repository": "https://github.com/blockapps/track-and-trace",
  "author": "BlockApps Inc.",
  "license": "MIT",
  "dependencies": {
    "blockapps-rest": "git://github.com/blockapps/blockapps-rest#SER-36_oauth-utils",
    "body-parser": "^1.18.3",
    "chai": "^4.2.0",
    "cookie-parser": "^1.4.3",
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "express-winston": "^3.0.1",
    "fs": "^0.0.1-security",
    "http-status-codes": "^1.3.2",
    "ip": "^1.1.5",
    "js-yaml": "^3.12.2",
    "jwt-decode": "^2.2.0",
    "mocha": "^6.0.2",
    "moment": "^2.24.0",
    "winston": "^3.2.0"
  },
  "scripts": {
    "mocha-babel": "node_modules/.bin/mocha --require @babel/register",
    "deploy": "cp config/${SERVER:-localhost}.config.yaml config.yaml && yarn mocha-babel dapp/dapp/dapp.deploy.js --config config/${SERVER:-localhost}.config.yaml -b",
    "start": "babel-node index.js",
    "token-getter": "node node_modules/blockapps-rest/lib/util/oauth.client.js --flow authorization-code",
    "test:asset": "yarn mocha-babel dapp/asset/test --config config/${SERVER:-localhost}.config.yaml -b",
    "test:bid": "yarn mocha-babel dapp/bid/test --config config/${SERVER:-localhost}.config.yaml -b",
    "test:bidChain": "yarn mocha-babel dapp/bidChain/test --config config/${SERVER:-localhost}.config.yaml -b",
    "test:e2e": "yarn mocha-babel test/v1 --config config/${SERVER:-localhost}.config.yaml -b",
    "test:permissions": "yarn mocha-babel dapp/ttPermission/test/ttPermissionManager.test.js --config config/${SERVER:-localhost}.config.yaml -b",
    "test": "yarn test:asset && yarn test:bidChain && yarn test:bid && yarn test:permissions && yarn test:e2e"
  },
  "devDependencies": {
    "chai-http": "^4.2.1",
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.4.0",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.4.2",
    "@babel/register": "^7.4.0"
  }
}
